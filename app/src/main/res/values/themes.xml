<resources>

    <!-- Base application theme. -->
    <style name="Theme.MyWebView" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="progressStyle" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:minHeight">8dp</item>
        <item name="android:progressDrawable">@drawable/selector_progress</item>
    </style>

</resources>